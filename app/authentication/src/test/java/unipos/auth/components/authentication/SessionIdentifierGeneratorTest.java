/*
package unipos.auth.components.authentication;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.AbstractJUnit4SpringContextTests;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.test.context.web.WebAppConfiguration;
import unipos.auth.config.MongoConfiguration;
import unipos.auth.config.web.SecurityConfiguration;
import unipos.auth.config.web.WebConfiguration;

import static org.junit.Assert.*;

public class SessionIdentifierGeneratorTest {

    SessionIdentifierGenerator sessionIdentifierGenerator = new SessionIdentifierGenerator();

    @Test
    public void testNextSessionId() throws Exception {
        assertNotNull(sessionIdentifierGenerator.nextSessionId());
        assertEquals(26,sessionIdentifierGenerator.nextSessionId().getBytes().length);
    }
}*/
